<%= form_for(@dhcp_vendor) do |f| %>
  <% if @dhcp_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dhcp_vendor.errors.count, "error") %> prohibited this dhcp_vendor from being saved:</h2>

      <ul>
      <% @dhcp_vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

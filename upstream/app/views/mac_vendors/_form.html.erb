<%= form_for(@mac_vendor) do |f| %>
  <% if @mac_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mac_vendor.errors.count, "error") %> prohibited this mac_vendor from being saved:</h2>

      <ul>
      <% @mac_vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mac %><br>
    <%= f.text_field :mac %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
